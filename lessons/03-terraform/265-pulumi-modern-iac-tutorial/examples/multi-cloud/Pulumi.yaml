name: pulumi-multi-cloud-example
runtime: nodejs
description: Multi-cloud infrastructure example with Pulumi

config:
  environment:
    description: Environment name (dev, staging, prod)
    default: dev
  region:
    description: AWS region
    default: us-west-2
  gcpProject:
    description: GCP project ID
    secret: false
